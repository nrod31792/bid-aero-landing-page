<div class="container">
  <%= form_for(@landingpage) do |f| %>
    <% if @landingpage.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@landingpage.errors.count, "error") %> prohibited this landingpage from being saved:</h2>

        <ul>
        <% @landingpage.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
<p id="notice"><%= notice %></p>
    <div class="field">
      <%= f.label :company %><br>
      <%= f.text_field :company %>
    </div>
    <div class="field">
      <%= f.label :name %><br>
      <%= f.text_field :name %>
    </div>
    <div class="field">
      <%= f.label :email %><br>
      <%= f.text_field :email %>
    </div>
    <div class="actions">
      <%= f.submit "Submit!" %>
    </div>
  <% end %>
</div>
